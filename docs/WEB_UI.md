# Web UI

The Web UI is optional.

## Features

- ChatGPT-like interface
- Real-time streaming
- Audio playback (OPUS/MP3)
- Audio interruption on input

The UI consumes the same `/v1` API as external clients.
